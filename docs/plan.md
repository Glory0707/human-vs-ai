# human-vs-ai 实施计划

> 配合 design.md。每个任务带验收标准；每个里程碑结束做一次走查。
> 以下为里程碑流水账（旧→新），当前能力清单以 [README](../README.md) 为准，轮次要点速览见 design.md §6。

---

## M1 · 引擎核心 + 学术规则库 ✅ 已完成（2026-09-20）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T1.1 | 包骨架（pyproject / human_vs_ai / tests / tools / docs） | `pip install -e .` 成功，CLI 可用 | ✅ |
| T1.2 | segment：句末标点切句、引号内不切、MD 结构行剥离 | 单测 6 项（引号/省略号/代码块/表格/空输入） | ✅ |
| T1.3 | stats：句长 CV、MATTR、连接词密度、4-gram、破折号密度 | 单测 4 项；jieba 可选缺位降级 | ✅ |
| T1.4 | engine：四层规则 + weak-alone 共现 + doc 阈值判定 | 单测 6 项（命中/降级/升级/shape/NaN 安全） | ✅ |
| T1.5 | report：同句聚合；terminal/md/json 三出口 | AI fixture 21 处命中、人类 fixture 0 高 0 中 | ✅ |
| T1.6 | 学术规则库 20 条（含解释/例句/出处） | `explain L-INFL-01` 输出完整说明 | ✅ |

## M2 · 真实语料校准 ✅ 已完成（2026-09-20）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T2.1 | 评测脚本（tools/evaluate.py / evaluate_cred.py） | AUROC + 规则区分度表 + 阈值扫描自动产出 | ✅ |
| T2.2 | HC3-Chinese 评测（问答文体基线） | 各 120 篇；词表 AUROC 0.558、TTR 0.800 | ✅ |
| T2.3 | C-ReD paper 评测（学术主场） | 真人 80 vs 四模型 320；词表 0.803、CV 0.798 | ✅ |
| T2.4 | 按评测校准：删 S-TRIAD-01 / L-SAFE-01 / D-TTR-01；D-CONN 阈值 0.45→0.08 | 校准后真人误命中 0.210→0.053，词表 AUROC 0.626→0.803 | ✅ |
| T2.5 | 砍掉清单入档（design.md §3，防复活） | 三条被证伪规则及数据写进 design/rules 文档 | ✅ |

**M2 出口验收**：校准数字可复现（`python tools/evaluate_cred.py`）；测试全绿；README 的实测区分度表与评测输出一致。

## M3 · 产品化收尾 ✅ 已完成（2026-09-20）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T3.1 | 文档四件套（README / design / rules / plan） | 与实现一致；无"计划中"功能写成已实现 | ✅ |
| T3.2 | 边界测试：空/纯代码块/超长无标点/英文/CRLF+BOM/2000 段重复 | 9 项边界测试全绿（合计 29 项） | ✅ |
| T3.3 | 性能：8685 字符（含 jieba）766 ms、285 句 | 达标（指标：<2s） | ✅ |
| T3.4 | .gitignore + LICENSE(MIT) + 首次提交 | 语料与第三方文本片段不入库 | ✅ |
| T3.5 | 走查协议：C-ReD 真人摘要 ×2 + gpt-4o 摘要 ×2 + README 自检 | 每处命中人工核对 | ✅（见下） |

### 走查记录（2026-09-20，逐处人工核对）

| 样本 | 修复前 | 核对结论 | 修复 |
|---|---|---|---|
| 真人医学摘要 #0 | 2 处（含 1 high 误伤） | "为肿瘤靶向治疗提供新的思路"是医学摘要体裁常规，真人高频——误伤 | "提供新思路"类从 L-INFL-01(high) 拆出并入 L-TAIL-01(medium) |
| 真人护理摘要 #1 | 2 处（1 medium） | "有效缓解疼痛"提示成立（该给出证据） | 保留 |
| gpt-4o 摘要 #0/#1 | 各 2/6 处 | 命中全部成立（揭示…机制 / CV 均匀 / 连接词密度） | 保留 |
| 同上 #1 弱命中 | L-EVADE 来源存疑 | 排查为"具有重要意义"在 L-INFL/L-EVADE 双词表重复覆盖 | 删 L-EVADE 重复模式，拔高归 L-INFL |
| D-NGRAM 阈值 | 真人 0.105 > 阈值 0.06 | 真人术语重复高，阈值误伤 | 0.06→0.15，标注观察期 |
| README 自检 | 1 low + 2 弱 | 全部为介绍规则的语境用词，提示合理 | — |

**走查后回归**：真人 2 篇 0 误伤；C-ReD 词表 AUROC 0.803→0.804（修复零损失）；29 项测试全绿。

**M3 出口验收**：走查清单全过 + 修复全部回归验证 + 文档与实现一致。

## M4 · 多文体扩展

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T4.1 | general profile（问答/科普/自媒体）：G-TRIAD 复活 + 万能开场/互动尾巴/热词/保险腔 | HC3 上 G-TRIAD +0.22、G-SAFE +0.06 生效；当代特征（开场/尾巴/热词）grep 验证语料频率 <0.2%，诚实标注"数字待当代语料" | ✅（2026-09-20） |
| T4.2 | 公文 profile：独立人类基线（公文体天然工整，防误伤） | ✅ 真公文误报率 0/15 = 0%（验收 <20% PASS）；语料=中国政府网公开公文（tools/fetch_gov_corpus.py）；首轮 100% 误报 → 三处设计修正（O-STK 转 lexicon / D-NGRAM 删除 / min_sentences 保护）；AI 样本 7 处命中逐条核对成立 | ✅（2026-09-20） |
| T4.3 | 完整论文正文的阈值分档（摘要 vs 正文） | ✅ C-ReD paper+news 双域分档分析：真人 CV p50 随长度 0.467→0.520 上移，D-UNIF 改三档阈值 0.30/0.33/0.37（各档锚定真人 p10≈10% 误报）；news 真人误报 18.9%→16.5%；数据/脚本入库（_qa/length-tiers.md、tools/analyze_length_tiers.py） | ✅（2026-09-20） |
| T4.4 | 纯前端网页版（单文件 index.html，浏览器打开即用，可离线） | 浏览器端到端实测（粘贴即析 13 处命中与 CLI 一致、profile 切换、零 console 错误）+ Python/JS 双引擎一致性测试 14/14 | ✅（2026-09-20） |

**T4.1 校准记录**：general@HC3 词表 AUROC 0.601（2023 语料对当代词表不友好，如实记录）；CV AUROC 0.763。方法论收获：词表必须 grep 语料验证（G-INTERACT 扩词表后命中率不动，grep 才发现表达频率为 0）；反向信号也是信号（"总之"17 倍差使 G-SAFE 生效、连接词密度反向使 general 不设 D-CONN）。

**T4.4 实现注记**：JS 引擎（web/engine.js）与 Python 包同构，`tools/check_web_consistency.py` 用 7 段固定语料 × 2 profile 逐字段 diff（findings/hints 全字段、stats 舍入 4 位；ttr 因浏览器无 jieba 口径不同而豁免，两端规则库均无 TTR 判定）。一致性测试首跑即抓到三处真实漂移：JS 正则缺 g 标志（replace 只删第一个标点）、引号嵌套处理缺 ASCII 引号奇偶逻辑、NaN 序列化差异。网页产物剥离维护者注（校准注/走查记录），CLI explain 保留完整解释——表现层各自决定，事实源唯一。

## M5 · 编辑器插件与长期演进（按需启动）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T5.1 | VS Code 扩展：当前文档一键分析 + Webview 报告面板，纯本地 | node 冒烟 7/7（三 profile 规则注入/引擎命中/HTML 生成/空输入安全）；报告预览视觉审查通过；手动验收：目录放 `.vscode/extensions` → 重载窗口 → 命令面板「human-vs-ai: 分析当前文档」→ 旁边面板出报告 | ✅（2026-09-20） |

**T6.1 口味校准层实现注记**（2026-09-20）：私库事件流 → `tools/extract_private_corpus.py`（源目录只读，产出全在 `corpus_private/`）→ 标注链 31 被毙 / 37 定稿 / 8 亲改 → 归纳 `docs/taste_zhouao.md`（T1–T12 口味条目 + R1–R5 改写准则）→ 落成 `personal` profile（规则带 `taste` 字段）+ `human_vs_ai/rewrite.py`（CLI `rewrite` 子命令 + 网页「改写建议」模式，双端同构由一致性探针守护）。先验对账：快照口径中位 191 字（先验 188，偏差 1.6%）、中英混用 76.2%（先验 73.7%）。验收：被毙召回 31/31、定稿误报 0/37、改写维度 5/6。自检项目自身文案 33 条，命中 1 条已修；R3 因自检发现误伤面过大（47/53）从判据降为提示。

**T8.4 端到端与视觉审查轮注记**（2026-09-22，v0.12.4）：computer use 黑盒端到端 + 2560px 视觉审查，11 个测试点覆盖四档切换/双模式/复制反馈/大文本竞态/暗色/窄屏/边界文本（空、行分隔符、闭引号、重复句）/扩展 webview 预览，全程控制台零错误（剪贴板内容验证因 headless 权限不可读，以「已复制 ✓」反馈标签为准）。抓到并修复一个 UI bug：同段大量相同句折叠成一组时，组标题把规则 ID/名称逐条拼接（1200 个相同句曾渲染出整版 G-SAFE-01 + …，web 报告/Markdown 导出/CLI 三处同源），修复为标题去重（matches 本就已去重；扩展端逐条渲染无此问题），pytest 钉 60 重复句回归（93 测试 / 156 一致性 / 26 冒烟 / 口味回归 PASS），浏览器复验 25 重复句标题干净。截图证据在 gui-test-screenshots/（gitignore）。

**T8.3 测试轮注记**（2026-09-22，v0.12.3）：测试员视角再扫一轮，主攻双端一致性与竞态。对拍实证并修复三类真漂移（Python 为参照修 JS）：①孤立低代理（U+DC00 无高代理配对）——Python len 算 1 码点、JS cpLength 漏数（n_chars 19≠20），cpLength 改为「低代理仅在无配对时跳过」；②Python splitlines 行界全集（\u2028/\u2029/\v/\f/\x85/\x1c-\x1e/裸 \r）JS 只认 \r\n——段中行分隔符两端段落文本不同（\n vs \u2028），engine.js/rewrite.js 拆行正则与 splitlines 集合逐字符对齐；③句尾闭引号吸收——边界标点后的 ”』」 Python 并入本句、JS 漏掉（句长分布漂移），JS 补 SENT_TAIL 吸收集（与 segment.py 逐码点一致）。并发：网页大文本 rAF 任务只靠文本相等判过期，换档/换模式不改文本导致过期任务放行、6 万字重复全量分析——加 analyzeGen 代数守卫，浏览器实测切 profile 后分析恰执行 1 次。工程：一致性检查与对拍新增 4 类固化探针（156 项/53 探针），孤立代理进不了裸 UTF-8 文件与 node stdout（编码层替换成 U+FFFD）——探针 JSON 与失败打印走 ensure_ascii 转义通道，node 输出端把无配对代理项转义回 \uXXXX（成对代理原样，首版正则误伤 emoji 已修）；fit_score 空 rows 给干净报错。新增 pytest：句尾吸收、行界全集 \n 拼接、tiers 边界 600/599 含端点。验收：92 测试 / 156 一致性 / 53 探针零漂移 / 26 冒烟 / 口味回归 PASS / 防泄漏干净。

**T8.2 目录与文案清理轮注记**（2026-09-22，v0.12.2）：文件层——删除 VERSION（停在 0.12.0 的陈旧副本，零引用，版本唯一来源是 human_vs_ai/__init__.py）、_qa/_server_deps（17MB numpy wheel，服务器引导用的传输中间件，服务器拟合已完结，git rm）、本地 dist/（0.11.3 过期构建产物，防未来 PyPI 发布带错版本）。死字段——human_ref（引擎加载、测试钉存在性，但三端没有任何渲染出口用过它，YAML 里 13 条内容全是维护者校准数字且与 rules.md 重复）：从 Rule/load_rules/四份 YAML/测试一并删除。文案层（能不说明就不说明）——弱命中双重截断标记去一（web 标题"（列前 12 处）"与尾注"…等 3 处（略）"二选一，留尾注，尾注再丢"（略）"；CLI 单标记不动）；复制按钮 title 删除（与按钮文字重复）；扩展改写面板 docname"personal（我的口味）"→"我的口味"；扩展 rules.json 开始剥离校准注（build_vscode 复用 build_web._strip_calibration_notes，T7.4 只剥了网页版，扩展端是漏网之鱼）。文案锚点三条不动（免责/页脚/未校准评分）。验收：90 测试 / 140 一致性 / 26 冒烟 / 48 探针零漂移 / 口味回归 PASS / 防泄漏干净；浏览器实测检测与改写视图渲染正常、旧标记零残留。

**T8.1 复用收敛轮注记**（2026-09-22，v0.12.1）：按"删冗余代码与无用注释、算法求简洁/健壮/复用、功能零变化"再过一遍 v0.11.1 后新增的代码。核心是研究工具去重：auroc 四份副本（evaluate/evaluate_cred/fit_score_tiers/analyze_length_tiers，其中一份曾少 +0.5 常数）收敛为 evaluate_cred.auroc 单实现（补空集 NaN 兜底）；逻辑回归两份实现收敛为 fit_score_tiers.fit（fit_score 改 fit(balance=False)，与被删 fit_logistic 在合成数据上逐位一致、numpy/纯 Python 两路径一致）；fit_tiers_diag.py（一次性诊断，结论已在 T8.0）删除，缓存重建职责并入 fit_tiers_compare.py（缺失自动抽取，实跑精确复现现役长档系数）；fit_score.py 死常量 WEIGHT 删除。杂项：report.py 抽 _group_top/_taste_suffix 化简、engine.js 独句段判定重排（省无谓 cpLength）、build_vscode.py import re 归位、一致性检查两处陈旧 docstring 纠正（avg_sentence_len 已纳入对比）、CI/README"136 项"改 140。验收：90 测试 / 140 一致性 / 26 冒烟 / 48 探针零漂移 / 口味回归 PASS / 防泄漏干净 / 构建幂等。

**T8.0 打磨与分档轮注记**（2026-09-22，v0.12.0）：①评分长文分档——服务器全量抽取（C-ReD paper 4993 + news 5783，天翼云 tmux，numpy 向量化后秒级完成）；统一 holdout 对比后落地"长档（≥600 字）专用系数"（0.975 vs 全局 0.946，短档分档反而更差故只做长档），引擎加 `scoring.tiers`（Py+JS 同构，_pick_scoring/pickScoring，元字段取全局）。过程中修两个工具 bug：fit_score_tiers 自带 auroc 副本少 +0.5 常数（0.953 被报成 0.453）、分位数未滤 NaN；evaluate_cred.auroc 补 NaN 过滤（NaN 并列检测死循环）。②毕设实测（/d/bs2/paper，tex 转文本工具 _hva_tex2txt.py 留 bs2）：28841 字，长档前 92 分词汇主导、长档后 99——校准人群为摘要的边界实例，记入 design.md §7。③general 当代样本初测（30 篇模型自生成，19 有效块）：G-INTERACT 首获当代命中、G-TRIAD 跨时代稳定，数字入 rules.md §5。④网页动效系统：riseIn 入场（仅上下文切换，逐键不闪）、分析中呼吸、reduced-motion 全关、扩展同款；浏览器实测动画中途 opacity 0.88→1、reducedMotion 下 animationName=none。⑤CI 上线（pytest/防泄漏/构建新鲜度零 diff/一致性/冒烟）。验收：90 测试 / 140 一致性（新增长文探针走 tiers 路径）/ 26 冒烟全绿。

**T7.5 测试轮注记**（2026-09-20，v0.11.3）：测试员视角全量找 bug。方法：48 探针对抗性输入双端对拍（_qa/drift_battery.py，跑完自清理）+ CLI 异常路径逐个过 + 性能边界梯度计时 + 浏览器重入实测。抓到三类真 bug 并修复：①JS UTF-16 码元 vs Python 码点（emoji/扩展区汉字上 n_chars/句长/独句段/TTR 全漂移，JS 改 cpLength/Array.from）；②空文本 avg_sentence_len Py=0.0 vs JS=NaN（一致性检查此前豁免该字段，掩盖漂移；统一 NaN 并纳入对比）；③正则灾难性回溯——EMPH 无界懒惰匹配（未闭合星号+4 万字 51.4s）与 EMAIL `[\w.+-]+@`（Python \w 认 CJK，长 CJK 串 12.7s，且两端 \w 语义不同）；修法为内容上限（EMPH 49+1+49、LINK/IMG 300/500）与 EMAIL 显式 ASCII 类 + RFC 5321 上限，修复后全部 ≤0.11s。CLI：-o 不可写路径、坏 YAML 干净报错。网页：双 rAF 过期任务丢弃、防抖清除、复制按钮定时器竞态三处修复。新增对抗探针固化进一致性检查（100→136 项）。验收：88 测试 / 136 一致性 / 26 冒烟全绿，48 探针零漂移，口味回归 PASS，防泄漏干净。

**T7.4 文案减负轮注记**（2026-09-20，v0.11.2）：清理全部用户可见文案——tooltip/占位符/空态/免责/弱命中/改写页脚/toast/状态栏，"能不说明就不说明"。保留三个核心短句作为立场锚点："风格提示，不是 AI 判定。"（免责）、"重要数据和结论要保留；梗得人来补。"（R1）、"该文体未校准评分"（score_note）；其余说明性从句全部删除（含指数 tooltip 里的 AUROC 方法论、"仅供参考"、"宁缺毋滥"、占位符里的切分说明）。注意 tests/test_personal.py 以子串钉住页脚、smoke 以"不是 AI 判定"/"该文体未校准评分"钉住关键文案——改文案先查这三处。目录清理：_qa 一次性实验脚本（exp_score.py/refit_general.py）删除、结论已记录在 T7.2；vscode-preview.html 与截图转 gitignore 可再生产物；_qa/ 只留评测记录（eval-*.md/json、length-tiers.*、score-fit.json）与本地语料。验收：84 测试 / 100 一致性 / 26 冒烟全绿，口味回归 PASS，视觉验收两视图 pass。

**T7.3 清理轮注记**（2026-09-20，v0.11.1）：按"删冗余代码与无用注释、算法求简洁/健壮/复用、功能零变化"做全系统清理。要点：修 activate() 缺 vscode require 的激活即崩 bug（v0.9.0 引入，冒烟盲区，补 Module._load 桩回归测试）；新建 web/render.js 共享渲染层（esc/fmt/hiSentence/评分行/弱命中块/常量，模板与扩展各删平行实现），segment.py 抽 _text_blocks（两出口共用块组装），engine.py 抽 _finding + load_rules 加 lru_cache，cli.py 抽 _emit；engine.js/rewrite.js 导出面裁到实际使用（analyze/rewriteText），删零引用的 splitParagraphs/stripMarkdown/split_paragraphs；build_web 内联转义收窄到 "</script"（全量 "</" 替换会打碎 esc() 的 /</g 正则，首测实录）。验收：84 测试 / 100 一致性 / 26 冒烟全绿，口味回归 PASS，防泄漏干净，视觉验收两视图 pass（零视觉变化）。

**T7.2 口径统一轮注记**（2026-09-20，v0.11.0）：全功能自查发现的毛边清单逐项落地。内核：jieba 退场——字级 2-gram 从"评分专用"升格为全文唯一切分口径（统计行/评分/一致性对比三处共用，装没装库数字都一样；一致性检查不再豁免 ttr）；CLI 指数两位小数改整数（假精度）；general 评分类平衡重拟合（HC3 真人占比 24% 导致截距虚高、人味文本普遍偏高分——类别倒数加权后 0.834→0.838、留出 0.868→0.870，真人 p50/p90 60/88→27/69；特征/类平衡实验含 6 特征变体，因纯 Python 拟合在本机耗时过长放弃，结论"特征集不动"，一次性实验脚本已清理、结论记录在本节与 rules.md §8）；TTR 单独区分度按新口径如实重标（HC3 391 篇 0.684，替换词级旧值 0.808）。表层：够 8 句却无分的档位三端给一行"未校准"说明（engine.score_note 双端一致纳入对比）；pyproject 移除 nlp extra。验收：84 测试 / 100 一致性 / 25 冒烟全绿，口味回归 PASS，防泄漏干净；排队：academic 短文分数摆放与按长度分档拟合（design.md §7）。

**T7.1 综合评分轮注记**（2026-09-20，v0.10.0）：用户提出"要综合宏观判断，不要碰词就判"。先验证后落地：tools/fit_score.py 四臂消融证明综合（逻辑回归合成规则密度+全文统计）AUROC 0.984/0.834（分层留出 0.974/0.868），纯扣词只有 0.847/0.577（问答语料≈瞎猜）——用户直觉被数字证实。评分落地为"AI 味指数"（0-100，报告首行+各特征贡献构成），系数进 YAML scoring 段；三个设计决策：规则特征用未门控密度（门控管逐句指控、聚合保幅度）、TTR 用字级 2-gram 评分专用口径（三端同分，代价 general 0.924→0.834 如实标注）、official/personal 不出分（无校准语料宁缺毋滥）。验收：82 测试 / 100 一致性（含 score 逐字段）/ 24 冒烟全绿，口味回归 PASS，评分行视觉 pass。

**T6.5 报告减负轮注记**（2026-09-20，v0.9.1）：用户实测超短输入后反馈"冗余说明多、不知道该看哪里"。三处减法（三端同步）：短文本（<8 句，与统计判定 min_sentences 同口径）统计只留规模一行（一两句话里 CV 全是"—"、TTR 恒 1，展示即噪音）；删统计行"（人类基线 ≈0.45，越低越平）"括号注释；"孤立弱命中"改"弱命中"（引擎术语不进界面）。验收：77 测试 / 100 一致性 / 20 冒烟全绿；短/长文本两视图 visual-judge pass，主判定回归视觉主角。

**T6.4 打磨轮注记**（2026-09-20，v0.9.0）：表层（交互流畅/美观/排版）与内核（完整/速度/质量）并进，每项改动带实测证据：
1. **内核**：MATTR 滚动窗口 O(n·w)→O(n)（10 万 token 96ms→22ms，随机语料逐位等价有单测）；两端规则正则按引用缓存（WeakMap，网页逐键分析不再重复编译）；改写器 R1 数字判据显式列全角 ０-９（JS `\d` 不认全角，"已完成３轮内测。"此前漏保；加一致性探针，口味回归 31/31 不退）。
2. **网页表层**：暗色模式（prefers-color-scheme 变量整套）；命中词在原句 <mark> 高亮（按索引区间切原文再转义，规避"先转义后拼串"被 mark 撞标签）；原句不再截 66 字；报告一键复制 Markdown（clipboard API + execCommand 兜底，检测/改写两种出口）；改写建议按删/改/留过滤 chips；弱命中只列前 12 处；overflow-wrap 防长 URL 溢出（500+ 字符无断行串实测零溢出，桌面+移动）；>6 万字先显示"分析中"再算、>3 万字防抖 300→800ms。
3. **扩展**：报告/改写面板颜色全部改 --vscode-* 主题变量（暗色主题不再白底刺眼，vscode-dark 类调 severity 色）；两条命令都把命中句按严重级画波浪线到正文（locateFindings 纯函数顺序定位+前缀兜底，node 冒烟直测；装饰句柄随命令重建防泄漏，失败静默降级不影响报告）；原句不截断、命中高亮、弱命中截断与网页同口径。
4. **CLI**：check/stats 支持 `-` stdin 管道（与 rewrite 同口径）；terminal/md 弱命中列表截断同三端，JSON 事实源永不截断（有单测钉住）。
验收：77 测试、100 一致性、17 冒烟全绿；口味回归 PASS（31/31、0/37、5/6）；浏览器实测 10 万字分析 233ms、全交互零 console 报错；五视图（桌面/暗色/移动/改写过滤/扩展预览）visual-judge 全 pass。

**T6.3 实用性轮注记**（2026-09-20，v0.8.0）：证据驱动，先跑三库评测锁基线（C-ReD AUROC 0.804/0.799、G-TRIAD +0.22、公文 0/15），修四类真实缺陷：
1. **切分器块模型**（segment.py 重写，Python+JS 同构）：列表项与表格行内容从"整块丢弃"改为独立成句参与分析（实证：三条 bullet 的知乎体只活下来 2 段 28 字，核心句式全漏）；标题/代码/分隔线仍剥离；新增裸 URL/邮箱剥离、星号强调（含中文才剥，不碰 3*5）、编号列表 `1.`/`1、` 一致性（旧代码"带空格丢、不带留还带编号噪声"）；列表/表格块豁免独句段形状规则。双端新增 5 条分析探针 + 1 条改写探针，一致性 68 → 96 项。
2. **personal T1 泛化**：补通用 LLM 关怀腔六类模式（"你已经很棒了/好好爱自己/你值得/我会一直陪着你/照顾好自己/一切都会好"），定稿 37 + 亲改 8 零误触门控；修改写器"建议整句删却标成改"的判档矛盾（机械结果明示整句删 → 删档，T-VOICE-05/口号类长句一并修正）；空铺垫半句（"无论结果如何。"）不再截断保留。
3. **三端可用性**：网页 profile 中文化（学术/问答/公文/我的口味 + 悬停场景说明），改写模式自动锁定 personal 且其余场景变灰禁用，词汇行补 TTR、无词表时不悬空"条/句"；扩展新增「改写建议（个人口味）」命令与面板（package.json 场景枚举补 personal）；CLI 坏场景四个子命令统一干净报错，stats 子命令只出统计（原来夹带完整 findings）。
验收：73 测试、96 一致性、13 冒烟全绿；三库复评区分度逐位不退（0.804/0.799、+0.22、0/15，公文"一、（一）"编号未被误切成列表）；自身文案自检 153 条零真实命中；四视图视觉 pass。

**T6.2 文案减负轮注记**（2026-09-20，v0.7.1）：免责语三端统一压成一行（"风格提示，不是 AI 判定；单条命中不构成证据。"）；统计行括号注释、发现汇总尾缀、弱命中标题、改写页脚、保留档理由（不再复述"R1/47 vs 53"等开发史）全部收紧；占位符两行并一行。规则解释删开发史段（拆分经过、2023 语料注三处去重），修 general D-UNIF 解释 0.526 与 human_ref 0.521 的数字矛盾；personal 两条解释里的 Markdown `**` 伪影清除。死代码：`_ASSERTION`、空 CSS 规则、`strip_markdown` 错误返回注解。`stats.py` 标点清洗从每句 4 次合并为 1 次（fixture 逐字段对比零漂移，TTR 口径刻意不动）；`_clean_prose` 扩到破折号/引号边界，折叠空格测试加档。`web/template.html` 源码混入的原始 NUL 字节改 `\u0000` 转义。验收：64 测试绿（折叠空格测试加档）、一致性 68 项、冒烟 8 项、口味回归 PASS、自身文案自检 130 条零真实命中、三视图视觉 pass。

**T5.1 实现注记**：扩展直接 require web/engine.js（24 项一致性背书）+ 构建时注入 rules.json（`tools/build_vscode.py`，与 build_web 同一事实源）；vscode 模块延迟 require 使报告渲染逻辑可被 node 冒烟直测；构建产物入库（clone 即用，零 npm 依赖）。

### M5 排队（按需启动）

- **当代自媒体语料校准 general**（抓取路径评估，2026-09）：
  a) 公开渠道抓取：知乎/公众号正文无开放 API，网页反爬强、平台条款风险高——**不作为默认路径**
  b) 可行替代：本地 LLM 生成"当代风格"样本（有自我偏差，须标注）；用户自愿提交匿名样本积累（隐私优先，符合项目定位）
  c) 已有资产：RUC 215 条特征编码与社区共识支撑词表迭代，量化 AUROC 待真实语料
- **公文真实 AI 样本扩充**（当前构造样本有自我偏差，已诚实标注）
- era 词频漂移季度重挖 · 句级困惑度插件（本地 Qwen，可选依赖）· Obsidian 插件（engine.js 直接复用）· 观点反复解释检测（语义层）
