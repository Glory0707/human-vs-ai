# human-vs-ai 实施计划

> 配合 design.md。每个任务带验收标准；每个里程碑结束做一次走查。
> 以下为里程碑流水账（旧→新），当前能力清单以 [README](../README.md) 为准。

## 现状速览（2026-09-20 · v0.2.0）

- **引擎**：MD 清洗 → 分段分句 → 词表/句式/形状/统计四层规则 → 共现加权 → 三格式报告
- **规则库**：academic 17 条（词表 10 / 句式 2 / 统计 5），全部带解释、例句、出处
- **校准**：C-ReD paper（词表 AUROC 0.803、句长 CV 0.798）+ HC3-Chinese（TTR 0.800，方向反转证据）
- **质量**：20 项单元+区分度测试全绿

---

## M1 · 引擎核心 + 学术规则库 ✅ 已完成（2026-09-20）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T1.1 | 包骨架（pyproject / human_vs_ai / tests / tools / docs） | `pip install -e .` 成功，CLI 可用 | ✅ |
| T1.2 | segment：句末标点切句、引号内不切、MD 结构行剥离 | 单测 6 项（引号/省略号/代码块/表格/空输入） | ✅ |
| T1.3 | stats：句长 CV、MATTR、连接词密度、4-gram、破折号密度 | 单测 4 项；jieba 可选缺位降级 | ✅ |
| T1.4 | engine：四层规则 + weak-alone 共现 + doc 阈值判定 | 单测 6 项（命中/降级/升级/shape/NaN 安全） | ✅ |
| T1.5 | report：同句聚合；terminal/md/json 三出口 | AI fixture 21 处命中、人类 fixture 0 高 0 中 | ✅ |
| T1.6 | 学术规则库 20 条（含解释/例句/出处） | `explain L-INFL-01` 输出完整说明 | ✅ |

## M2 · 真实语料校准 ✅ 已完成（2026-09-20）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T2.1 | 评测脚本（tools/evaluate.py / evaluate_cred.py） | AUROC + 规则区分度表 + 阈值扫描自动产出 | ✅ |
| T2.2 | HC3-Chinese 评测（问答文体基线） | 各 120 篇；词表 AUROC 0.558、TTR 0.800 | ✅ |
| T2.3 | C-ReD paper 评测（学术主场） | 真人 80 vs 四模型 320；词表 0.803、CV 0.798 | ✅ |
| T2.4 | 按评测校准：删 S-TRIAD-01 / L-SAFE-01 / D-TTR-01；D-CONN 阈值 0.45→0.08 | 校准后真人误命中 0.210→0.053，词表 AUROC 0.626→0.803 | ✅ |
| T2.5 | 砍掉清单入档（design.md §3，防复活） | 三条被证伪规则及数据写进 design/rules 文档 | ✅ |

**M2 出口验收**：校准数字可复现（`python tools/evaluate_cred.py`）；测试全绿；README 的实测区分度表与评测输出一致。

## M3 · 产品化收尾 ✅ 已完成（2026-09-20）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T3.1 | 文档四件套（README / design / rules / plan） | 与实现一致；无"计划中"功能写成已实现 | ✅ |
| T3.2 | 边界测试：空/纯代码块/超长无标点/英文/CRLF+BOM/2000 段重复 | 9 项边界测试全绿（合计 29 项） | ✅ |
| T3.3 | 性能：8685 字符（含 jieba）766 ms、285 句 | 达标（指标：<2s） | ✅ |
| T3.4 | .gitignore + LICENSE(MIT) + 首次提交 | 语料与第三方文本片段不入库 | ✅ |
| T3.5 | 走查协议：C-ReD 真人摘要 ×2 + gpt-4o 摘要 ×2 + README 自检 | 每处命中人工核对 | ✅（见下） |

### 走查记录（2026-09-20，逐处人工核对）

| 样本 | 修复前 | 核对结论 | 修复 |
|---|---|---|---|
| 真人医学摘要 #0 | 2 处（含 1 high 误伤） | "为肿瘤靶向治疗提供新的思路"是医学摘要体裁常规，真人高频——误伤 | "提供新思路"类从 L-INFL-01(high) 拆出并入 L-TAIL-01(medium) |
| 真人护理摘要 #1 | 2 处（1 medium） | "有效缓解疼痛"提示成立（该给出证据） | 保留 |
| gpt-4o 摘要 #0/#1 | 各 2/6 处 | 命中全部成立（揭示…机制 / CV 均匀 / 连接词密度） | 保留 |
| 同上 #1 弱命中 | L-EVADE 来源存疑 | 排查为"具有重要意义"在 L-INFL/L-EVADE 双词表重复覆盖 | 删 L-EVADE 重复模式，拔高归 L-INFL |
| D-NGRAM 阈值 | 真人 0.105 > 阈值 0.06 | 真人术语重复高，阈值误伤 | 0.06→0.15，标注观察期 |
| README 自检 | 1 low + 2 弱 | 全部为介绍规则的语境用词，提示合理 | — |

**走查后回归**：真人 2 篇 0 误伤；C-ReD 词表 AUROC 0.803→0.804（修复零损失）；29 项测试全绿。

**M3 出口验收**：走查清单全过 + 修复全部回归验证 + 文档与实现一致。

## M4 · 多文体扩展

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T4.1 | general profile（问答/科普/自媒体）：G-TRIAD 复活 + 万能开场/互动尾巴/热词/保险腔 | HC3 上 G-TRIAD +0.22、G-SAFE +0.06 生效；当代特征（开场/尾巴/热词）grep 验证语料频率 <0.2%，诚实标注"数字待当代语料" | ✅（2026-09-20） |
| T4.2 | 公文 profile：独立人类基线（公文体天然工整，防误伤） | ✅ 真公文误报率 0/15 = 0%（验收 <20% PASS）；语料=中国政府网公开公文（tools/fetch_gov_corpus.py）；首轮 100% 误报 → 三处设计修正（O-STK 转 lexicon / D-NGRAM 删除 / min_sentences 保护）；AI 样本 7 处命中逐条核对成立 | ✅（2026-09-20） |
| T4.3 | 完整论文正文的阈值分档（摘要 vs 正文） | ✅ C-ReD paper+news 双域分档分析：真人 CV p50 随长度 0.467→0.520 上移，D-UNIF 改三档阈值 0.30/0.33/0.37（各档锚定真人 p10≈10% 误报）；news 真人误报 18.9%→16.5%；数据/脚本入库（_qa/length-tiers.md、tools/analyze_length_tiers.py） | ✅（2026-09-20） |
| T4.4 | 纯前端网页版（单文件 index.html，浏览器打开即用，可离线） | 浏览器端到端实测（粘贴即析 13 处命中与 CLI 一致、profile 切换、零 console 错误）+ Python/JS 双引擎一致性测试 14/14 | ✅（2026-09-20） |

**T4.1 校准记录**：general@HC3 词表 AUROC 0.601（2023 语料对当代词表不友好，如实记录）；CV AUROC 0.763。方法论收获：词表必须 grep 语料验证（G-INTERACT 扩词表后命中率不动，grep 才发现表达频率为 0）；反向信号也是信号（"总之"17 倍差使 G-SAFE 生效、连接词密度反向使 general 不设 D-CONN）。

**T4.4 实现注记**：JS 引擎（web/engine.js）与 Python 包同构，`tools/check_web_consistency.py` 用 7 段固定语料 × 2 profile 逐字段 diff（findings/hints 全字段、stats 舍入 4 位；ttr 因浏览器无 jieba 口径不同而豁免，两端规则库均无 TTR 判定）。一致性测试首跑即抓到三处真实漂移：JS 正则缺 g 标志（replace 只删第一个标点）、引号嵌套处理缺 ASCII 引号奇偶逻辑、NaN 序列化差异。网页产物剥离维护者注（校准注/走查记录），CLI explain 保留完整解释——表现层各自决定，事实源唯一。

## M5 · 编辑器插件与长期演进（按需启动）

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T5.1 | VS Code 扩展：当前文档一键分析 + Webview 报告面板，纯本地 | node 冒烟 7/7（三 profile 规则注入/引擎命中/HTML 生成/空输入安全）；报告预览视觉审查通过；手动验收：目录放 `.vscode/extensions` → 重载窗口 → 命令面板「human-vs-ai: 分析当前文档」→ 旁边面板出报告 | ✅（2026-09-20） |

**T6.1 口味校准层实现注记**（2026-09-20）：私库事件流 → `tools/extract_private_corpus.py`（源目录只读，产出全在 `corpus_private/`）→ 标注链 31 被毙 / 37 定稿 / 8 亲改 → 归纳 `docs/taste_zhouao.md`（T1–T12 口味条目 + R1–R5 改写准则）→ 落成 `personal` profile（规则带 `taste` 字段）+ `human_vs_ai/rewrite.py`（CLI `rewrite` 子命令 + 网页「改写建议」模式，双端同构由一致性探针守护）。先验对账：快照口径中位 191 字（先验 188，偏差 1.6%）、中英混用 76.2%（先验 73.7%）。验收：被毙召回 31/31、定稿误报 0/37、改写维度 5/6。自检项目自身文案 33 条，命中 1 条已修；R3 因自检发现误伤面过大（47/53）从判据降为提示。

**T6.4 打磨轮注记**（2026-09-20，v0.9.0）：表层（交互流畅/美观/排版）与内核（完整/速度/质量）并进，每项改动带实测证据：
1. **内核**：MATTR 滚动窗口 O(n·w)→O(n)（10 万 token 96ms→22ms，随机语料逐位等价有单测）；两端规则正则按引用缓存（WeakMap，网页逐键分析不再重复编译）；改写器 R1 数字判据显式列全角 ０-９（JS `\d` 不认全角，"已完成３轮内测。"此前漏保；加一致性探针，口味回归 31/31 不退）。
2. **网页表层**：暗色模式（prefers-color-scheme 变量整套）；命中词在原句 <mark> 高亮（按索引区间切原文再转义，规避"先转义后拼串"被 mark 撞标签）；原句不再截 66 字；报告一键复制 Markdown（clipboard API + execCommand 兜底，检测/改写两种出口）；改写建议按删/改/留过滤 chips；弱命中只列前 12 处；overflow-wrap 防长 URL 溢出（500+ 字符无断行串实测零溢出，桌面+移动）；>6 万字先显示"分析中"再算、>3 万字防抖 300→800ms。
3. **扩展**：报告/改写面板颜色全部改 --vscode-* 主题变量（暗色主题不再白底刺眼，vscode-dark 类调 severity 色）；两条命令都把命中句按严重级画波浪线到正文（locateFindings 纯函数顺序定位+前缀兜底，node 冒烟直测；装饰句柄随命令重建防泄漏，失败静默降级不影响报告）；原句不截断、命中高亮、弱命中截断与网页同口径。
4. **CLI**：check/stats 支持 `-` stdin 管道（与 rewrite 同口径）；terminal/md 弱命中列表截断同三端，JSON 事实源永不截断（有单测钉住）。
验收：77 测试、100 一致性、17 冒烟全绿；口味回归 PASS（31/31、0/37、5/6）；浏览器实测 10 万字分析 233ms、全交互零 console 报错；五视图（桌面/暗色/移动/改写过滤/扩展预览）visual-judge 全 pass。

**T6.3 实用性轮注记**（2026-09-20，v0.8.0）：证据驱动，先跑三库评测锁基线（C-ReD AUROC 0.804/0.799、G-TRIAD +0.22、公文 0/15），修四类真实缺陷：
1. **切分器块模型**（segment.py 重写，Python+JS 同构）：列表项与表格行内容从"整块丢弃"改为独立成句参与分析（实证：三条 bullet 的知乎体只活下来 2 段 28 字，核心句式全漏）；标题/代码/分隔线仍剥离；新增裸 URL/邮箱剥离、星号强调（含中文才剥，不碰 3*5）、编号列表 `1.`/`1、` 一致性（旧代码"带空格丢、不带留还带编号噪声"）；列表/表格块豁免独句段形状规则。双端新增 5 条分析探针 + 1 条改写探针，一致性 68 → 96 项。
2. **personal T1 泛化**：补通用 LLM 关怀腔六类模式（"你已经很棒了/好好爱自己/你值得/我会一直陪着你/照顾好自己/一切都会好"），定稿 37 + 亲改 8 零误触门控；修改写器"建议整句删却标成改"的判档矛盾（机械结果明示整句删 → 删档，T-VOICE-05/口号类长句一并修正）；空铺垫半句（"无论结果如何。"）不再截断保留。
3. **三端可用性**：网页 profile 中文化（学术/问答/公文/我的口味 + 悬停场景说明），改写模式自动锁定 personal 且其余场景变灰禁用，词汇行补 TTR、无词表时不悬空"条/句"；扩展新增「改写建议（个人口味）」命令与面板（package.json 场景枚举补 personal）；CLI 坏场景四个子命令统一干净报错，stats 子命令只出统计（原来夹带完整 findings）。
验收：73 测试、96 一致性、13 冒烟全绿；三库复评区分度逐位不退（0.804/0.799、+0.22、0/15，公文"一、（一）"编号未被误切成列表）；自身文案自检 153 条零真实命中；四视图视觉 pass。

**T6.2 文案减负轮注记**（2026-09-20，v0.7.1）：免责语三端统一压成一行（"风格提示，不是 AI 判定；单条命中不构成证据。"）；统计行括号注释、发现汇总尾缀、弱命中标题、改写页脚、保留档理由（不再复述"R1/47 vs 53"等开发史）全部收紧；占位符两行并一行。规则解释删开发史段（拆分经过、2023 语料注三处去重），修 general D-UNIF 解释 0.526 与 human_ref 0.521 的数字矛盾；personal 两条解释里的 Markdown `**` 伪影清除。死代码：`_ASSERTION`、空 CSS 规则、`strip_markdown` 错误返回注解。`stats.py` 标点清洗从每句 4 次合并为 1 次（fixture 逐字段对比零漂移，TTR 口径刻意不动）；`_clean_prose` 扩到破折号/引号边界，折叠空格测试加档。`web/template.html` 源码混入的原始 NUL 字节改 `\u0000` 转义。验收：64 测试绿（折叠空格测试加档）、一致性 68 项、冒烟 8 项、口味回归 PASS、自身文案自检 130 条零真实命中、三视图视觉 pass。

**T5.1 实现注记**：扩展直接 require web/engine.js（24 项一致性背书）+ 构建时注入 rules.json（`tools/build_vscode.py`，与 build_web 同一事实源）；vscode 模块延迟 require 使报告渲染逻辑可被 node 冒烟直测；构建产物入库（clone 即用，零 npm 依赖）。

### M5 排队（按需启动）

- **当代自媒体语料校准 general**（抓取路径评估，2026-09）：
  a) 公开渠道抓取：知乎/公众号正文无开放 API，网页反爬强、平台条款风险高——**不作为默认路径**
  b) 可行替代：本地 LLM 生成"当代风格"样本（有自我偏差，须标注）；用户自愿提交匿名样本积累（隐私优先，符合项目定位）
  c) 已有资产：RUC 215 条特征编码与社区共识支撑词表迭代，量化 AUROC 待真实语料
- **公文真实 AI 样本扩充**（当前构造样本有自我偏差，已诚实标注）
- era 词频漂移季度重挖 · 句级困惑度插件（本地 Qwen，可选依赖）· Obsidian 插件（engine.js 直接复用）· 观点反复解释检测（语义层）
